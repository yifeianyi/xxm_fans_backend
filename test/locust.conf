# locust.conf

# 主机地址
host = https://www.xxm8777.cn

# 用户数（并发量）
users = 100

# 启动速率（每秒新增用户数）
spawn-rate = 15

# 运行时长
run-time = 10m

# 无头模式（不启 web UI）
headless = true

# 结果输出
csv = load_test_results
